abstract class Url {
  static const String base = 'https://api.github.com';
  static const String search = '/search';

  //LOGIN
  static const String repositories = base + search + '/repositories';
}
