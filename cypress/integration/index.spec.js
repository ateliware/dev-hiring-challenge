/// <reference types="cypress" />

describe('Rewards dashboard', () => {
  
  beforeEach(() => {
    cy.visit('/')
  })

  it('should load page and languages properly', () => {
    cy.get('select')
  })

})