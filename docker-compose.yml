version: '3.8'
services:
  db:
    image: "postgres:13.3"
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      POSTGRES_DB: "flaskapp_db"
    volumes:
      - "dbdata:/var/lib/postgresql/data"
  flaskapp:
    build: .
    volumes:
      - .:/opt/services/flaskapp/src
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      POSTGRES_DB: "flaskapp_db"
      APP_SECRET_KEY: "ateliware"
    depends_on:
      - db
volumes:
  dbdata:
